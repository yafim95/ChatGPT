{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability for the ProjectMind main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
